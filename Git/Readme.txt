// Transformation dossier local en dossier Git
git init 

// Incorper les tous les fichiers (par défaut) dans git (stage)
git add

// Incorper les tous les fichiers (par défaut) dans git (stage)
git commit -m "comment text"

// afficher les changements
git log -p 
